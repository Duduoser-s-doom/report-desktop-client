import { shallow } from "enzyme"
import { Constructor } from "./Constructor"

describe("Test Constructor component",()=>{
    let wrapper = shallow(<Constructor />)
    beforeEach(()=>{
        wrapper=shallow(<Constructor/>)
    })
})